{{- if .Values.httpRoute.enabled }}
HTTPRoute {{ include "duynh.fullname" . }} was created.

Check whether Envoy Gateway accepted it:
  kubectl get httproute {{ include "duynh.fullname" . }} -n {{ .Release.Namespace }} -o wide
{{- else }}
The service is internal. To access it locally:
  kubectl port-forward -n {{ .Release.Namespace }} service/{{ include "duynh.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}
