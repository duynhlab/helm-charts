Grafana dashboards have been deployed as ConfigMaps.

Ensure your Grafana has sidecar enabled with:
  sidecar.dashboards.enabled: true
{{- range $key, $value := .Values.sidecar.labels }}
  sidecar.dashboards.label: {{ $key }}
  sidecar.dashboards.labelValue: {{ $value | quote }}
{{- end }}

Deployed dashboards:
{{- range $name, $dashboard := .Values.dashboards }}
{{- if $dashboard.enabled }}
  - {{ $name }}{{ if $dashboard.folder }} (folder: {{ $dashboard.folder }}){{ end }}
{{- end }}
{{- end }}
